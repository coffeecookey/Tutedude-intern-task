// Some constants for the app server
const TICK_RATE = 50;
const MAP_BOUNDS = { width: 2000, height: 2000 };
const PROXIMITY_RADIUS = 400;
const MAX_SPEED = 8;
const SPAWN_POSITION = { x: 350, y: 300 };

module.exports = { TICK_RATE, MAP_BOUNDS, PROXIMITY_RADIUS, MAX_SPEED, SPAWN_POSITION };
